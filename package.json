{
  "name": "oir-test-new",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && node scripts/build-index.mjs",
    "start": "next start -p ${PORT:-3000}",
    "lint": "next lint"
  }
}
